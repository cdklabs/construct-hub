// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

export interface ReStagePackageVersionProps extends lambda.FunctionOptions {
}

export class ReStagePackageVersion extends lambda.Function {
  constructor(scope: Construct, id: string, props?: ReStagePackageVersionProps) {
    super(scope, id, {
      description: 'package-sources/npmjs/re-stage-package-version.lambda.ts',
      ...props,
      architecture: lambda.Architecture.ARM_64,
      runtime: lambda.Runtime.NODEJS_16_X,
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '/re-stage-package-version.lambda.bundle')),
    });
  }
}