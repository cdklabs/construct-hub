// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

export interface PackageStatsProcessorProps extends lambda.FunctionOptions {
}

export class PackageStatsProcessor extends lambda.Function {
  constructor(scope: Construct, id: string, props?: PackageStatsProcessorProps) {
    super(scope, id, {
      description: 'backend/package-stats/package-stats-processor.lambda.ts',
      ...props,
      architecture: lambda.Architecture.ARM_64,
      runtime: lambda.Runtime.NODEJS_22_X,
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '/package-stats-processor.lambda.bundle')),
    });
  }
}