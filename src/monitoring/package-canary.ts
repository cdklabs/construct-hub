// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from '@aws-cdk/aws-lambda';
import { Construct } from '@aws-cdk/core';

export interface PackageCanaryProps extends lambda.FunctionOptions {
}

export class PackageCanary extends lambda.Function {
  constructor(scope: Construct, id: string, props?: PackageCanaryProps) {
    super(scope, id, {
      description: 'monitoring/package-canary.lambda.ts',
      ...props,
      runtime: lambda.Runtime.NODEJS_14_X,
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '/package-canary.lambda.bundle')),
    });
  }
}